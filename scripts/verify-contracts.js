const hre = require("hardhat"); async function verifyContract(address, constructorArgs) { try { await hre.run("verify:verify", { address: address, constructorArguments: constructorArgs }); console.log("Contract verified successfully"); } catch (error) { console.error("Verification failed:", error); } } async function main() { const contracts = require("../deployments/contracts.json"); await verifyContract(contracts.CrossChainMessenger, [contracts.Router, contracts.WETH, ethers.parseUnits("10", "ether"), ethers.parseUnits("100", "ether"), 86400]); } main().then(() => process.exit(0)).catch((error) => { console.error(error); process.exit(1); });
