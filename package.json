{
  "name": "crosschainethbridge",
  "version": "1.0.0",
  "description": "Cross-chain messaging solution that bridges ETH from Defi Oracle Meta Mainnet to Polygon PoS",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "lint": "solhint 'contracts/**/*.sol'",
    "coverage": "hardhat coverage"
  },
  "keywords": ["ethereum", "polygon", "bridge", "chainlink", "ccip"],
  "author": "Defi Oracle Meta",
  "license": "MIT",
  "devDependencies": {
    "@chainlink/contracts-ccip": "^1.5.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.16"
  }
}
